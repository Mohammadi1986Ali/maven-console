# maven-console

A small multi-module Maven console application.

## Modules

- `maven-console-app` - runnable console application packaged as a jar.

## Run

After packaging, run the application with:

```shell
java -jar maven-console-app/target/maven-console-1.0.1.jar
```
